# services

Frontend API service layer.

## Files

- `api.js`: pool and dataset write requests plus shared API base URL helpers

This layer intentionally keeps request paths and payloads aligned with the existing backend API.
