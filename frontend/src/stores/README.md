# stores

Pinia stores live here.

## Files

- `app.js`: WebSocket lifecycle, live snapshot cache, refresh actions, and frontend connection state

This store replaces the old module-singleton state implementation while keeping the same backend protocol behavior.
